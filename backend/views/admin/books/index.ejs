<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        <%- include('../../partials/adminHeader') %>
        :root {
            --primary-blue: #2563eb;
            --secondary-blue: #3b82f6;
            --success-green: #10b981;
            --warning-orange: #f59e0b;
            --danger-red: #ef4444;
        }

        body {
            background-color: #f8fafc;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        /* Cards and Components */
        .stat-card {
            background: white;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
            border: none;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            overflow: hidden;
            position: relative;
        }

        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--primary-blue), var(--secondary-blue));
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 30px rgba(0,0,0,0.12);
        }

        .stat-card .stat-number {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--primary-blue);
            margin: 0;
        }

        .stat-card .stat-label {
            color: #64748b;
            font-size: 1rem;
            margin: 0;
            font-weight: 500;
        }

        .stat-card .stat-icon {
            font-size: 3rem;
            opacity: 0.7;
        }

        .stat-card.books .stat-icon { color: var(--primary-blue); }
        .stat-card.categories .stat-icon { color: var(--success-green); }
        .stat-card.pages .stat-icon { color: var(--warning-orange); }
        .stat-card.current .stat-icon { color: var(--danger-red); }

        /* Filters */
        .filters-section {
            padding: 0 30px 20px;
        }

        .filter-card {
            background: white;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 2px 15px rgba(0,0,0,0.05);
            border: 1px solid #e2e8f0;
        }

        /* Table */
        .table-section {
            padding: 0 30px 30px;
        }

        .table-card {
            background: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
            border: none;
        }

        .table-header {
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            padding: 20px 25px;
            border-bottom: 2px solid #e2e8f0;
        }

        .table-header h5 {
            margin: 0;
            color: #1e293b;
            font-weight: 600;
        }

        .custom-table {
            margin: 0;
        }

        .custom-table th {
            background-color: #f8fafc;
            color: #1e293b;
            font-weight: 600;
            padding: 15px;
            border-bottom: 2px solid #e2e8f0;
            font-size: 0.9rem;
        }

        .custom-table td {
            padding: 15px;
            vertical-align: middle;
            border-bottom: 1px solid #f1f5f9;
        }

        .custom-table tbody tr:hover {
            background-color: #f8fafc;
        }

        .book-cover {
            width: 50px;
            height: 70px;
            object-fit: cover;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.15);
        }

        .book-title {
            font-weight: 600;
            color: #1e293b;
            margin-bottom: 4px;
        }

        .book-author {
            color: #64748b;
            font-size: 0.9rem;
        }

        .category-badge {
            background: linear-gradient(135deg, var(--primary-blue), var(--secondary-blue));
            color: white;
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 500;
        }

        .price-display {
            color: var(--success-green);
            font-weight: 600;
            font-size: 1.1rem;
        }

        .stock-badge {
            padding: 6px 12px;
            border-radius: 15px;
            font-weight: 500;
            font-size: 0.9rem;
        }

        .stock-badge.high {
            background-color: #dcfce7;
            color: #166534;
        }

        .stock-badge.medium {
            background-color: #fef3c7;
            color: #92400e;
        }

        .stock-badge.low {
            background-color: #fee2e2;
            color: #991b1b;
        }

        /* Action Buttons */
        .action-buttons .btn {
            border-radius: 8px;
            padding: 8px 12px;
            margin: 0 2px;
            font-size: 0.85rem;
        }

        .btn-view { 
            background-color: #f1f5f9; 
            color: #475569; 
            border: 1px solid #cbd5e1;
        }
        .btn-view:hover { 
            background-color: #e2e8f0; 
            color: #334155; 
        }

        .btn-edit { 
            background-color: #fef3c7; 
            color: #92400e; 
            border: 1px solid #fcd34d;
        }
        .btn-edit:hover { 
            background-color: #fde68a; 
            color: #78350f; 
        }

        .btn-delete { 
            background-color: #fee2e2; 
            color: #991b1b; 
            border: 1px solid #fca5a5;
        }
        .btn-delete:hover { 
            background-color: #fecaca; 
            color: #7f1d1d; 
        }

        /* Pagination */
        .pagination {
            margin-top: 30px;
        }

        .page-link {
            color: var(--primary-blue);
            border: 1px solid #e2e8f0;
            padding: 10px 15px;
            margin: 0 3px;
            border-radius: 8px;
        }

        .page-link:hover {
            background-color: var(--primary-blue);
            border-color: var(--primary-blue);
            color: white;
        }

        .page-item.active .page-link {
            background-color: var(--primary-blue);
            border-color: var(--primary-blue);
        }

    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <%- include('../../partials/adminSidebar') %>

            <!-- Main Content -->
            <div class="col-md-10">
                <div class="d-flex justify-content-between align-items-center py-3 border-bottom">
                    <h1><%= title %></h1>
                    <div>
                        <a href="/admin/books/create" class="btn btn-primary me-2">
                            <i class="fas fa-plus"></i> Thêm sách mới
                        </a>
                        <span class="badge bg-primary">Quản trị viên: <%= currentUser.username %></span>
                    </div>
                </div>

                <!-- Flash Messages -->
                <% if (typeof success_msg !== 'undefined' && success_msg.length > 0) { %>
                    <div class="alert alert-success alert-dismissible fade show mt-3">
                        <%= success_msg %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                <% } %>

                <% if (typeof error_msg !== 'undefined' && error_msg.length > 0) { %>
                    <div class="alert alert-danger alert-dismissible fade show mt-3">
                        <%= error_msg %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                <% } %>

                <!-- Statistics Cards -->
                <div class="row mt-4 g-3">
                    <div class="col-md-3">
                        <div class="card stats-card" style="background: linear-gradient(135deg,#667eea 0%,#764ba2 100%);">
                            <div class="card-body">
                                <h6 class="text-uppercase mb-1">Tổng số sách</h6>
                                <h2 class="fw-bold"><%= totalBooks %></h2>
                                <p class="mb-0"><i class="fas fa-book me-2"></i>Tất cả sách</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card stats-card" style="background: linear-gradient(135deg,#43e97b 0%,#38f9d7 100%);">
                            <div class="card-body">
                                <h6 class="text-uppercase mb-1">Tổng danh mục</h6>
                                <h2 class="fw-bold"><%= categories.length %></h2>
                                <p class="mb-0"><i class="fas fa-tags me-2"></i>Danh mục sách</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card stats-card" style="background: linear-gradient(135deg,#4facfe 0%,#00f2fe 100%);">
                            <div class="card-body">
                                <h6 class="text-uppercase mb-1">Tổng số trang</h6>
                                <h2 class="fw-bold"><%= totalPages %></h2>
                                <p class="mb-0"><i class="fas fa-file-alt me-2"></i>Trang <%= currentPage %> / <%= totalPages %></p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card stats-card" style="background: linear-gradient(135deg,#f093fb 0%,#f5576c 100%);">
                            <div class="card-body">
                                <h6 class="text-uppercase mb-1">Trang hiện tại</h6>
                                <h2 class="fw-bold"><%= currentPage %></h2>
                                <p class="mb-0"><i class="fas fa-bookmark me-2"></i>Hiển thị <%= books.length %> sách</p>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Filters -->
                <div class="card mt-4">
                    <div class="card-body">
                <form method="GET" class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label fw-semibold">Tìm kiếm</label>
                        <input type="text" class="form-control" name="search" 
                               value="<%= searchQuery %>" placeholder="Tiêu đề, tác giả...">
                    </div>
                    <div class="col-md-4">
                        <label class="form-label fw-semibold">Danh mục</label>
                        <select name="category" class="form-select">
                            <option value="all" <%= categoryFilter === 'all' ? 'selected' : '' %>>Tất cả</option>
                            <% categories.forEach(cat => { %>
                                <option value="<%= cat._id %>" <%= categoryFilter === cat._id.toString() ? 'selected' : '' %>>
                                    <%= cat.name %>
                                </option>
                            <% }) %>
                        </select>
                    </div>
                    <div class="col-md-2">
                        <label class="form-label">&nbsp;</label>
                        <button type="submit" class="btn btn-primary w-100">
                            <i class="fas fa-search"></i> Tìm kiếm
                        </button>
                    </div>
                </form>
            </div>
        </div>

                <!-- Book Table -->
                <div class="card mt-4">
                    <div class="card-header">
                        <h5 class="mb-0">Danh sách sách</h5>
                    </div>
                    <div class="card-body p-0">
                
                        <% if (books.length > 0) { %>
                            <div class="table-responsive">
                                <table class="table table-hover mb-0">
                            <thead>
                                <tr>
                                    <th>STT</th>
                                    <th>Bìa</th>
                                    <th>Thông tin sách</th>
                                    <th>Danh mục</th>
                                    <th>Giá</th>
                                    <th>Số lượng</th>
                                    <th>Ngày tạo</th>
                                    <th>Thao tác</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% books.forEach((book, index) => { %>
                                    <tr>
                                        <td><strong><%= (currentPage - 1) * 10 + index + 1 %></strong></td>
                                        <td>
                                            <% if (book.coverImage) { %>
                                                <img src="<%= book.coverImage.startsWith('http') ? book.coverImage : '/uploads/' + book.coverImage %>" 
                                                     alt="Bìa sách" class="book-cover">
                                            <% } else { %>
                                                <div class="book-cover bg-light d-flex align-items-center justify-content-center">
                                                    <i class="fas fa-book text-muted"></i>
                                                </div>
                                            <% } %>
                                        </td>
                                        <td>
                                            <div class="book-title"><%= book.title %></div>
                                            <div class="book-author">bởi <%= book.author %></div>
                                            <% if (book.averageRating > 0) { %>
                                                <div class="text-warning mt-1">
                                                    <% for (let i = 1; i <= 5; i++) { %>
                                                        <i class="fas fa-star<%= i <= book.averageRating ? '' : ' text-muted' %>"></i>
                                                    <% } %>
                                                    (<%= book.totalReviews %>)
                                                </div>
                                            <% } %>
                                        </td>
                                        <td>
                                            <span class="category-badge">
                                                <%= book.category?.name || 'Chưa phân loại' %>
                                            </span>
                                        </td>
                                        <td>
                                            <div class="price-display">
                                                <%= book.price.toLocaleString('vi-VN') %>.750₫
                                            </div>
                                            <% if (book.coinPrice) { %>
                                                <small class="text-warning">
                                                    <i class="fas fa-coins"></i> <%= book.coinPrice %> xu
                                                </small>
                                            <% } %>
                                        </td>
                                        <td>
                                            <% let stockClass = book.stock <= 5 ? 'low' : book.stock <= 20 ? 'medium' : 'high' %>
                                            <span class="stock-badge <%= stockClass %>">
                                                <%= book.stock || '0' %>
                                            </span>
                                        </td>
                                        <td>
                                            <small><%= new Date(book.createdAt).toLocaleDateString('vi-VN') %></small>
                                        </td>
                                        <td>
                                            <div class="action-buttons d-flex">
                                                <a href="/books/<%= book._id %>" class="btn btn-view" target="_blank" title="Xem">
                                                    <i class="fas fa-eye"></i>
                                                </a>
                                                <a href="/admin/books/<%= book._id %>/edit" class="btn btn-edit" title="Sửa">
                                                    <i class="fas fa-edit"></i>
                                                </a>
                                                <form method="POST" action="/admin/books/<%= book._id %>/delete" class="d-inline">
                                                    <button type="submit" class="btn btn-delete" title="Xóa"
                                                            onclick="return confirm('Bạn có chắc muốn xóa sách này?')">
                                                        <i class="fas fa-trash"></i>
                                                    </button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                <% } else { %>
                    <div class="text-center py-5">
                        <i class="fas fa-book fa-4x text-muted mb-3"></i>
                        <h5 class="text-muted">Không tìm thấy sách nào</h5>
                        <p class="text-muted">Thử thay đổi điều kiện tìm kiếm hoặc thêm sách mới</p>
                        <a href="/admin/books/create" class="btn btn-primary btn-lg">
                            <i class="fas fa-plus"></i> Thêm sách đầu tiên
                        </a>
                    </div>
                        <% } %>
                    </div>
                </div>

                <!-- Pagination -->
                <% if (totalPages > 1) { %>
                    <nav class="mt-4">
                        <ul class="pagination justify-content-center">
                            <% if (currentPage > 1) { %>
                                <li class="page-item">
                                    <a class="page-link" href="?page=<%= currentPage - 1 %>&search=<%= searchQuery %>&category=<%= categoryFilter %>">
                                        <i class="fas fa-chevron-left"></i> Trước
                                    </a>
                                </li>
                            <% } %>
                            
                            <% for (let i = Math.max(1, currentPage - 2); i <= Math.min(totalPages, currentPage + 2); i++) { %>
                                <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                                    <a class="page-link" href="?page=<%= i %>&search=<%= searchQuery %>&category=<%= categoryFilter %>">
                                        <%= i %>
                                    </a>
                                </li>
                            <% } %>
                            
                            <% if (currentPage < totalPages) { %>
                                <li class="page-item">
                                    <a class="page-link" href="?page=<%= currentPage + 1 %>&search=<%= searchQuery %>&category=<%= categoryFilter %>">
                                        Sau <i class="fas fa-chevron-right"></i>
                                    </a>
                                </li>
                            <% } %>
                        </ul>
                    </nav>
                <% } %>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>